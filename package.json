{
  "name": "event-platform",
  "version": "1.0.0",
  "description": "An AZLE ICP Canister for Events Management",
  "main": "index.js",
  "scripts": {
    "start": "dfx start --background",
    "stop": "dfx stop",
    "deploy": "dfx deploy",
    "redeploy": "npm run stop && npm run start:clean && npm run deploy",
    "start:clean": "dfx start --background --clean"
  },
  "author": "victor orlunda",
  "license": "ISC",
  "dependencies": {
    "@dfinity/agent": "^0.16.0",
    "@dfinity/candid": "^0.16.0",
    "azle": "^0.16.3",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "@types/uuid": "^9.0.2"
  },
  "engines": {
    "node": "^12 || ~14 || ^16 || ^18"
  }
}
